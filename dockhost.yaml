version: 1
project: telegram-ai-bot

containers:
  - name: telegram-bot
    image:
      context: .
    restart: always
    environment:
      TELEGRAM_BOT_TOKEN:
      PERPLEXITY_API_KEY:
      ADMIN_USER_IDS:
